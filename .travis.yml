dist: bionic

script:
  - sudo apt install -y build-essential
  - sudo apt install -y check
  # For some reason, sudo apt-get install -y check keeps failing on Travis
  #- wget https://github.com/libcheck/check/releases/download/0.10.0/check-0.10.0.tar.gz
  #- tar xvf check-0.10.0.tar.gz
  #- (cd check-0.10.0 && ./configure > /dev/null)
  #- (cd check-0.10.0 && make > /dev/null)
  #- (cd check-0.10.0 && make check > /dev/null)
  #- (cd check-0.10.0 && make install > /dev/null)
  #- make

compiler: gcc

language: c
